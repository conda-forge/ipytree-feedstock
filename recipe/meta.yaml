{% set name = "ipytree" %}
{% set version = "0.1.3" %}
{% set sha256 = "e8a4fef7356fb82971ab09135d38f244d1828785ed37c61acaa257088fed2973" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 1
  noarch: python
  script: "{{ PYTHON }} -m pip install . --no-deps -vv"

requirements:
  host:
    - python
    - setuptools
    - pip
  run:
    - python
    - ipywidgets >=7.5.0,<8.0.0

test:
  imports:
    - ipytree

about:
  home: http://github.com/QuantStack/ipytree
  license: MIT
  license_family: MIT
  license_file: LICENSE
  summary: 'A Jupyter Notebook Tree Widget using jsTree'
  dev_url: https://github.com/QuantStack/ipytree

extra:
  recipe-maintainers:
    - martinRenou
